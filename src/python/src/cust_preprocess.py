#from loguru import logger as log

def inward_parms_preprocessing(request_type, parms, config):
    # if request_type=='GET':
    #     log.debug('Inward parms for preprocessing %s' % parms)
    #     log.debug('Configurations from config_get.yaml %s' % config)
    return parms