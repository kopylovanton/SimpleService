# from loguru import logger as log

def outward_parms_preprocessing(request_type, parms, config):
    # if request_type=='POST':
    # log.debug('Outward parms for preprocessing %s' % parms)
    # log.debug('Configurations from config_get.yaml %s' % config)
    return parms
